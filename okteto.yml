deploy:
  - kubectl apply -f master

dev:
  web:
    command: ["source", "setup.sh"]
    sync:
      - .:/app
    services:
      - name: worker
        command: ["bash", "start.sh"]
        sync:
          - .:/app
